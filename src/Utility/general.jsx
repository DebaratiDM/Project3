export function areaOfRectangle(length, width) {
  const area = length * width;
  return area;
}
export function areaOfCircle(radius, Pie) {
  const circleArea = Pie * radius * radius;
  return circleArea;
}
